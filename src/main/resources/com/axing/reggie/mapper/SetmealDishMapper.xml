<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.axing.reggie.mapper.SetmealDishMapper">

    <insert id="save">
        insert into setmeal_dish
            (
                id,
                setmeal_id,
                dish_id,
                name,
                price,
                copies,
                create_time,
                update_time,
                create_user,
                update_user
            )
            values
            <foreach collection="list" item="sd" separator=",">
                (#{sd.id},#{sd.setmealId},#{sd.dishId},#{sd.name},#{sd.price},#{sd.copies},#{sd.createTime},#{sd.updateTime},#{sd.createUser},#{sd.updateUser})
            </foreach>
    </insert>

    <delete id="deleteBySetmealIds">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="list" item="ids" separator="," open="(" close=")">
            #{ids}
        </foreach>
    </delete>

    <select id="getSetmealIdByDishId" resultType="java.lang.Long">
        SELECT setmeal_id FROM `setmeal_dish` WHERE dish_id in
        <foreach collection="list" item="ids" separator="," open="(" close=")">
            #{ids}
        </foreach>
    </select>

    <select id="getSetmealDishBySetmealId" resultType="com.axing.reggie.domain.SetmealDish">
        select * from setmeal_dish where setmeal_id=#{setmealId}
    </select>

    <select id="getDishIdBySetmealId" resultType="java.lang.Long">
        select dish_id
        from setmeal_dish
        where setmeal_id=#{setmealId}
    </select>

    <select id="getCopies" resultType="java.lang.Integer">
        select copies from setmeal_dish where setmeal_id=#{setmealId} and dish_id=#{dishId}
    </select>


</mapper>
