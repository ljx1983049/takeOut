<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.axing.reggie.mapper.UserMapper">

    <insert id="addUser">
        insert into user (id,phone,name,password) values (#{id},#{phone},#{name},#{password})
    </insert>

    <select id="newUser" resultType="com.axing.reggie.domain.User">
        select * from user where phone=#{phone}
    </select>

    <select id="getUserById" resultType="com.axing.reggie.domain.User">
        select * from user where id=#{id}
    </select>

</mapper>
